# next-auth declares peerOptional nodemailer@^7, but we use nodemailer 9 (its
# Email provider is unused — we only use Credentials). Don't let that optional
# peer mismatch fail installs (locally or on Vercel).
legacy-peer-deps=true
